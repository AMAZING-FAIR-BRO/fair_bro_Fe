import { TouchableOpacity, StyleSheet } from 'react-native';
import { IconButton } from '../../atoms/IconButton/IconButton';

export function BottomTabItem(props: any) {
  return <IconButton {...props} />;
}
